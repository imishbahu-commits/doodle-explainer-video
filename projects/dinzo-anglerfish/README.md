# Why It Sucks to Be Born as an Anglerfish

Dinzo-style 16:9 doodle explainer. 114 beats / 12 parts. Part 1 (beats 1–10) is rendered.

```bash
python3 build.py part 1    # this batch
python3 build.py final     # after later parts exist
```

Watch in Studio (`python3 studio.py 8090`). Type `go` for the next 10 beats.

# Copy this file to `.env` and fill in your key.
# .env is gitignored — never committed, never pasted in chat.
QWEN_API_KEY=
# Only change these if your TokensRouter account uses different slugs:
QWEN_IMAGE_MODEL=qwen-image
QWEN_VIDEO_MODEL=wan-2.2-t2v
QWEN_I2V_MODEL=wan-2.2-i2v
